# 后端服务根地址（不含 /api/v1）
# Taro 会自动把以 TARO_APP_ 开头的环境变量注入到产物中。
#
# 本地开发：复制本文件为 .env 并按需修改
#   cp .env.example .env
# Vercel 部署：在 Project Settings → Environment Variables 中新增同名变量，
#   值填你的 Railway 后端域名（如 https://xxx.up.railway.app 或绑定的自定义域名）。
#   注意这是构建期注入，新增/修改后需重新触发一次部署才生效。
#
# 留空则回退到 src/constants/api.ts 里的默认地址。
TARO_APP_API_ORIGIN=https://jbsttj-backend-production.up.railway.app
