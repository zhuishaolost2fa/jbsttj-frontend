# 换行符策略：仓库里统一存 LF，checkout 时也不转 CRLF。
#
# 为什么需要：Windows 上 core.autocrlf=true 会在 checkout 时把 LF 转成 CRLF。
# 对 .sh 脚本是致命的 —— Linux 上执行带 \r 的脚本会报
#   /bin/bash^M: bad interpreter: No such file or directory
# 本项目的部署脚本最终在服务器（Linux）上跑，必须保证 LF。
#
# 同时 .sh 的可执行位也要在 Windows 上用
#   git update-index --chmod=+x scripts/xxx.sh
# 固化成 100755，否则提交的是 100644，服务器上 chmod +x 后 git 会误判为「未提交改动」。

* text=auto eol=lf

# 脚本必须 LF + 可执行
*.sh text eol=lf

# 二进制资源不参与换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
*.ttf binary
