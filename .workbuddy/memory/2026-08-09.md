# 2026-08-09 工作记录

## 前端对接 Railway 后端
- 后端已部署：`https://jbsttj-backend-production.up.railway.app`（FastAPI，/docs 可访问，返回 200）。
- 改动 `src/constants/api.ts`：`API_ORIGIN` 生产/开发环境均改为 `https://jbsttj-backend-production.up.railway.app`（用户确认「本地 dev 也走 railway」，故不再保留本地 127.0.0.1:8020）。
- 生产构建（`taro build --type h5`）与本地 dev（`npm run dev:h5`，默认 http://localhost:10086）都指向 Railway 后端。
- CORS 已打通：用户在 Railway 后端 `CORS_ORIGINS` 加上 `http://localhost:10086`。复测 OPTIONS 预检返回 `200` 且 `access-control-allow-origin: http://localhost:10086`，`POST /auth/login` 也回显正确 CORS 头（空体返回 422 属正常校验）。
- 生产构建已验证：`npm run build:h5` 成功（39s），产物 dist 中仅含 `https://jbsttj-backend-production.up.railway.app`，无 `api.your-domain.com` / `127.0.0.1:8020` 残留。
- 待办（用户侧）：① 前端上线域名需同样加入后端 `CORS_ORIGINS`（Vercel/Netlify 域名首次部署后确定）；② OSS Bucket CORS 也要放行 `http://localhost:10086` 与前端生产域名（直传分片走 OSS，非后端域名）。
