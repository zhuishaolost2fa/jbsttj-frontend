# 2026-08-17

- 收窄 DM 指南导入格式白名单：从 `PDF / Word（.pdf .doc .docx）` 改为仅支持 Word（`.doc` `.docx`）。
  - `src/constants/upload.ts` 的 `DM_GUIDE_ACCEPT`：移除 `.pdf` 与 `application/pdf`，label 改为 `Word（.doc .docx）`。
  - `src/utils/filePicker.ts`：`resolveContentType` 删除 `.pdf` 分支，更新注释。
  - `docs/dm-guide-upload-api.md`：同步「支持格式」说明为仅 Word。
  - 校验与 Content-Type 推导均依赖 `DM_GUIDE_ACCEPT`，无需改 `ImportDmGuide` 组件。
  - ⚠️ 后端 `content_type` 白名单若仍放 PDF 不会受影响（前端已拦截）；但后端若仅接受 Word 类型则一致。

- 剧本详情页聊天界面重设计：新增「问答目录」概览（走 `GET /scripts/{code}/dm-guide/qa-titles`，公开接口）。
  - 概览以**底部抽屉**独立呈现，不占用聊天消息流（符合「不要直接显示占用聊天框」要求）。
  - 入口两处：ready-bar 右侧「📖 问答目录」按钮 + 空聊天态引导链接「浏览问答目录」。
  - `src/services/dmGuide.ts`：新增 `QATitleItem / QATitleNode / QATitleChain` 类型与 `fetchQaTitleChain(idOrCode)`（路径式公开接口）。
  - `src/pages/scriptDetail/index.tsx`：`openCatalog`（懒加载 + 默认展开前两级 + 失败可重试）、`handleCatalogAsk`（点问答直接发起提问并收起抽屉）、`renderCatalogNodes` 递归标题树。
  - 抽屉用 `onClick={e => e.stopPropagation()}` 阻断遮罩关闭（Taro 4 的 ViewProps 未声明 `catchClick`，用 stopPropagation 可过 tsc）。
  - 验收：`tsc --noEmit` src 0 报错（node_modules 的 Taro 声明报错为既有问题）；`npm run build:h5` 成功。

- 目录抽屉交互优化（响应「关掉后找不到上次位置 + 无预加载体感慢」反馈）：
  - **后台预加载**：手册就绪（isReady）即 `loadCatalog()` 拉 /qa-titles，点开抽屉秒开；`openCatalog` 仅作兜底（loadCatalog 内部按 catalogFetched/catalogLoading 去重），失败重试也走 loadCatalog。
  - **抽屉常驻挂载**：`catalogMounted` 一旦为 true 不再卸载，用 `is-open` 类 + CSS transition（opacity/visibility/pointer-events + translateY）控制显隐——滚动位置与树展开状态原样保留；废弃 keyframe 动画（重挂载才触发）。
  - **上次提问高亮**：`lastAskedQaId` 记录点出去的问答，重开抽屉该条目带蓝框 +「上次提问」角标。
  - 注意：dev server（10086, watch 模式）锁定 `dist/`，此时 `npm run build:h5` 会在 `emptyOutputDir` 卡死/报错；验证改用 `curl /pages/scriptDetail/index.tsx|less` 看 vite 转换结果 + `tsc --noEmit`。
