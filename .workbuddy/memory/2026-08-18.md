# 2026-08-18

- 目录提问短路：问答目录条目自带预生成 `answer`，点「提问」不再调检索接口。
  - `handleCatalogAsk(item, sectionPath)` 直接把问+答落成消息：`source="manual"`、`similarity=1`、`matchedQuestion=item.question`，sources 带章节路径与页码（复用消息气泡的「查看出处」）。
  - `renderCatalogNodes` 调用处改传 `n.path`；`item.answer` 为空时防御性回退 `submitQuestion` 正常链路。
  - 效果：目录提问零网络请求、零延迟；输入框自由提问仍走向量检索/LLM 两级分流。
  - 验收：`tsc --noEmit` src 0 报错；vite `/pages/scriptDetail/index.tsx` 200 且含新逻辑。

- 聊天会话持久化（localStorage）+ 清除会话：
  - 存储键 `dm_chat_{scriptCode}`（按剧本隔离），结构 `{v, savedAt, lastAskedQaId, messages}`，最多保留最近 50 条防撑爆 5MB 额度；Taro H5 的 get/setStorageSync 底层即 localStorage。
  - **惰性初始化恢复**：`messages`/`lastAskedQaId` 用 useState 惰性初始值从存储读（首渲染 scriptCode 已可用），避免「effect 先写空数组覆盖存档再读档」的时序坑；`initialChatRef` 缓存防止重复读存储。
  - 消息/lastAskedQaId 变化即落盘（目录直答也覆盖）；恢复后 mount effect 把 scrollTarget 定位到最后一条。
  - 清除会话：ready-bar 右侧「清除会话」按钮（仅 messages 非空时显示），`Taro.showModal` 二次确认 → 清 state + `removeStorageSync`。
  - 存档读写全部 try/catch 静默降级，绝不影响聊天主流程。
  - 验收：`tsc --noEmit` src 0 报错；vite `/pages/scriptDetail/index.tsx` 200 且含 persistChat/handleClearChat。

- 用户提问列表接口对接（`dm_guides.py` 三接口）：
  - service 层 `dmGuide.ts` 新增 `QuestionRecord`/`QuestionListResult`/`GuideQuestions` 类型与 `fetchDmQuestions`/`answerDmQuestion`/`fetchGuideQuestions` 三个封装（均走扁平 `/dm-guide/` 前缀）。
  - 新建 `components/QuestionPanel`：底部抽屉，复用 catalog 抽屉的视觉语言（sheet-mask + translateY 动效）。
  - 交互：状态筛选 tabs（全部/待解答/已解答/已忽略）、问题卡片含提问者头像昵称+状态标签+被问次数、已解答可展开答案（含解答者信息）、待解答+已登录显示内联 Textarea 回答框（提交后原地更新无需重拉）、分页「加载更多」、点击问题可直接发到聊天框（`onAskQuestion` 回调 `submitQuestion`）。
  - 未登录点「我来回答」弹 modal 引导登录。
  - 详情页 ready-bar 新增「❓ 用户提问」按钮（`qpanel-btn`），与「📖 问答目录」并列。
  - 验收：`tsc --noEmit` src 0 报错；`taro build --type h5` 成功。
