# 2026-08-21

## 剧本库页改为「只展示已解析剧本」+ 后端 has_guide 过滤
- 需求：去除剧本库页的筛选 UI，只展示已解析完成的剧本（即已关联 DM 主持人手册）。
- 后端（jbsttj-backend）：
  - `app/api/v1/scripts.py` 的 `GET /scripts` 新增 `has_guide: Optional[bool]` 查询参数；
  - `app/services/script_service.py::list_scripts` 透传；
  - `app/services/repository.py::list_scripts` 实现过滤：`extra->dmGuide` 为 jsonb 路径，`has_guide=true` → `not.is.null`，`false` → `is.null`。
  - 三文件 py_compile 通过；需部署 Railway 后生效。
  - **踩坑修复**：初版误写 `is.not.null`，PostgREST 报 PGRST100（`unexpected "." expecting isVal`）；正确取反语法是 `not.is.null`（NOT 前置于 is）。已改为 `not.is.null` 并对 Supabase PostgREST 直连验证：旧语法 400，新语法正常返回 3 条已解析剧本。
- 前端（jbsttj-frontend）：
  - `src/services/script.ts`：`ScriptListQuery` 新增 `hasGuide?: boolean`，`fetchScriptList` 映射为 `has_guide` 参数；`fetchScriptOptions` 及字典类型保留（ScriptSubmitForm 仍在用）。
  - `src/pages/scripts/index.tsx`：整体重写，移除搜索框/排序 chip/玩法·题材·难度·人数筛选/已选汇总条/清除按钮等全部筛选状态与 UI；固定 `hasGuide: true, sort: "newest"` 请求；保留下拉刷新、加载更多、空/错状态；空态文案改为「暂无已解析剧本」；卡片去掉冗余「已导入」徽章。
  - `src/pages/scripts/index.less`：清理不再使用的搜索/排序/筛选/汇总条样式。
  - `src/app.config.ts`：tabBar 注释同步为「剧本库（只展示已解析完成的剧本）」。
  - 验收：`tsc --noEmit` 源码 0 报错（node_modules 内 @tarojs/components 类型噪音为既有问题）；`npm run build:h5` 成功（42s）。
- 注意：H5 dev server 未在运行（8010 无监听）；改动含 app.config.ts，重启 dev 后生效。

## 底部 tabBar 增加图标（保留内置方案）
- 需求：tabbar 原本只有文字，补齐图标；用户可提供 iconfont，最终决定保留内置图标。
- 新增 `scripts/gen-tabbar-icons.py`（PIL 绘制 + 4x 超采样 → LANCZOS 缩到 81x81）：上传箭头（导入）/ 合上书 + 书页线（剧本）/ 人形（我的），灰 #9aa0ae 与选中蓝 #5b7cfa 两态。
- 产物 6 个 PNG 放 `src/assets/tabbar/`（各约 2KB）；`src/app.config.ts` 三个 tab 均配置 `iconPath` / `selectedIconPath`，注释注明 iconfont 替换方式（覆盖同名文件即可）。
- 验证：`npx taro build --type h5` 成功（54s），图标被打进 `dist/static/images/tab-*.png`，产物中 tabBar 配置的 iconPath 被编译期重写为 `/static/images/...`（m[]/x[] 数组）——Taro 原生 tabBar 只认本地图片、不支持 iconfont 字体。
- 注意：手动 `rm -rf dist` 会触发沙箱 safe-delete 拦截；直接 `npx taro build --type h5` 即可（vite 自行 emptyOutDir）。
- dev:h5（watch，8010）已后台启动。
