# 2026-08-22

- 在 Ardot 画布（file 717496620475325）设计了两个单 path tabBar 图标：「我的」（人物轮廓）与「剧本列表」（圆角文档+三行列表线），提供选中 #5B7CFA / 默认 #9AA0AE 两种状态。
- 已导出并下载到 `src/assets/tabbar/`，命名沿用现有约定：`tab-profile{-active}.svg`、`tab-scripts{-active}.svg`（active=蓝，默认=灰）。每个文件仅含一条 path（导出时描边已展开为 fill 轮廓，fill-rule evenodd），尺寸 48×48。
- 注意：导出的 SVG 与原 PNG 共存于同目录，若 Taro tabBar 仍需 PNG（原生 tabBar 只接受本地图片），可用这些 SVG 转 81×81 PNG 替换。
- 将「导入 DM 手册」模块从首页（pages/index）迁入「我的」页面（pages/profile）：
  - app.config.ts tabBar list[0] 文案「导入」→「首页」，首页只保留品牌+搜索+剧透提示；移除首页的 ImportDmGuide / ScriptSubmitForm / 匹配提交流程及相关 state、imports。
  - 我的页新增导入卡片，含说明区：仅支持 Word（.doc/.docx）；PDF 需 OCR 成本高暂不支持；PDF→Word 用 Edge 打印模式拆分+夸克网盘转换；收益分配=剧本解析费 20%，其余给导入者与知识库共建者。导入成功后复用原「匹配剧本→填表→提交→跳我的剧本」链路。
  - 首页 index.less 删除已废弃的 import-section / import-locked 样式；profile/index.less 新增 import-card / import-hint / import-locked 样式。
  - tsc（src 内 0 报错）+ build:h5 成功（54s）。注意：`rm -rf dist` 仍触发 safe-delete 拦截，dist 被清空后直接 build 即可。
