# 2026-08-27

## 剧本详情页「故事还原」tab + 划线评论全量对接（H5/小程序）

后端接口来源：`jbsttj-backend/app/api/v1/dm_guides.py`（扁平 /dm-guide 系列，全部 camelCase alias）。

- **服务层** `src/services/dmGuide.ts` 新增：
  - 类型：`StoryItem / StoryDetail / StoryListResult / HighlightRecord / HighlightListResult / CreateHighlightPayload`；
  - 函数：`fetchStories(code,{storyType,limit,offset})`、`fetchStoryDetail(storyId,limit=50)`、`fetchHighlights({code,storyId,mine,...})`、`createHighlight / updateHighlight / deleteHighlight`；
  - `STORY_TYPE_TEXT / STORY_TYPE_TONE`（timeline/truth/role/clue/ending/other 中文文案与色系）。
- **新组件** `src/components/StoryPanel/`（列表 + 全屏阅读 overlay）：
  - 列表：类型筛选 chips + 卡片流（标题/摘要/章节/页码/公开划线数）+ 加载更多；懒加载（tab 首次激活才拉，公开接口未登录可读）。
  - 阅读页：正文 + `meta.events`（{when,what}）时间线事件 + 共读时间线（公开划线与我的私有划线按 id 去重合并、时间倒序，私有条目标「仅自己可见」）。
  - **H5 划线**：`document.selectionchange` 持续捕获选区（quote/offset/prefix/suffix 在选区收起前已入 state），底部按钮提交；offset 用 `Array.from` 按码点统计（与后端 Web Annotation 口径一致）；选区必须完整落在 `#story-content-dom` 内。小程序端无 selection 事件，只读。
  - 自己的划线支持编辑（评论/可见性 private↔public）与删除（软删二次确认）；提交成功后重拉详情合并。
- **scriptDetail 页**：头部下新增 `content-tabs`（💬 问答 / 📖 故事还原）；问答 tab 行为不变（status/ready-bar + 聊天 + 输入栏全部 `activeTab==='qa'` 守卫）；布局从 min-height 100vh + chat-area max-height calc 改为 `height:100vh` 弹性列 + `flex:1; min-height:0` 内部滚动。
- 验收：`npx tsc --noEmit` src/ 0 报错（node_modules 的 Taro 类型噪音为存量）；`NODE_OPTIONS= npm run build:h5` 成功（47s）。
- 踩坑记录：给原有 `{!isReady ? A : B}` 二元表达式前加 `&& activeTab==='qa'` 后必须补齐三元 else（`: null`），否则 TS1005。
