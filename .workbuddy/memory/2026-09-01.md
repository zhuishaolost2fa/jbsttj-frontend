# 2026-09-01

## SEO / GEO 静态内容站（构建期生成）

背景：H5 是 Taro SPA + hash 路由，首屏 HTML 空壳，爬虫/AI 抓不到任何内容。方案定为**构建期生成静态内容站**，域名 https://www.jbs-ttj.store，收录范围 = 元信息 + 故事还原 + 常见问答（不含完整手册原文）。

新增文件：
- `scripts/gen-seo-pages.mjs`（核心，~1000 行）：拉后端公开 API → 生成
  - `dist/scripts/index.html` 剧本库（45 部）
  - `dist/s/{code}/index.html` 内容页（13 部有 DM 手册的）
  - `dist/robots.txt` / `sitemap.xml` / `llms.txt` / `feed.xml`
  - 向 `dist/index.html` 注入 meta + JSON-LD + noscript 兜底（幂等，用 `data-seo-injected="1"` + `<!-- SEO:BEGIN/END -->` 标记）
  - 兜底把 `src/assets/og-image.png` 拷到 `dist/static/`
- `scripts/gen-og-image.js`：@resvg/resvg-js 渲染 1200×630 OG 分享图 → `src/assets/og-image.png`

改动：
- `package.json`：`build:h5` = `node scripts/gen-favicon.js && node scripts/gen-og-image.js && taro build --type h5 && node scripts/copy-favicon.js && node scripts/gen-seo-pages.mjs`
- `vercel.json`：加 `/scripts` 与 `/s/:code` 显式 rewrite
- `src/index.html`：只留基础 meta，社交元数据改由脚本注入（避免重复 og 标签）
- `src/hooks/usePageMeta.ts`：重写，增加 canonical / og:* / twitter:* / JSON-LD 运行时注入

## 踩过的坑（重要）

1. **Taro 不会把 `src/static/` 拷到 `dist/`**（与既往记忆相反）。OG 图必须靠 `gen-seo-pages.mjs` 构建后兜底拷贝 → 统一引用 `/static/og-image.png`。
2. **后端 `/scripts` 的 `limit` 上限 < 200**（传 200 返 422），必须分页（limit=50 循环）。`/dm-guide/stories` 同理。
3. **列表页会产生 32 个 404**：45 部剧本里只有 13 部生成了内容页。修法 —— `hrefOf(s)`：`hasGuide` 才链 `/s/{code}/`，否则链 SPA 路由 `/#/pages/scriptDetail/index?code=xxx`。改完用脚本扫全站 `href` 验证 0 死链。
4. **`src/index.html` 的 HTML 注释里不能嵌套 `<!--`**，否则注释提前结束。
5. 变量遮蔽：`const jsonLd = jsonLd({...})` 会遮蔽同名工具函数，已改名 `ldBlocks`。
6. resvg 的 SVG 属性：`font-family=${JSON.stringify(FONT)}` 会给属性值再套一层双引号，必须写 `font-family="${FONT}"`。
7. `gui-he-nu-fang` 的 qa-titles 响应慢（~30s+），脚本需带重试 + 并发控制。

## 已上线（19:45 确认）

commit `fd7e7e3 feat(seo)` 已推送 GitHub，Vercel 自动部署完成。线上实测全部 200 且 content-type 正确：
`/scripts/`、`/s/{code}/`（text/html）、`/llms.txt`（text/plain）、`/feed.xml`（application/xml）、`/sitemap.xml`（application/xml）、`/static/og-image.png`（image/png）。
首页 title/og:title 已生效。**Vercel 会优先命中静态文件，cactch-all rewrite 不会吞掉 sitemap.xml**（返回真 XML 而非 SPA HTML）。

## 验收结果

- 全站 15 个页面，JSON-LD 全部合法（WebSite / Organization / ItemList / CollectionPage / BreadcrumbList / FAQPage / Article）
- 站内链接 0 死链；关键路由全部 200
- `src/` tsc 0 错；完整 `npm run build:h5` 通过（需带 `NODE_OPTIONS=` 前缀绕过沙箱 trash shim）
- 新增内容站 1.3MB，dist 总计 2.5MB
