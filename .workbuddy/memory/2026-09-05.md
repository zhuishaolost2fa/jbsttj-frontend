# 2026-09-05

## SEO / GEO 重启：阶段 0（本地可做的部分）

背景：用户之前因无法上外网，卡在「登录境外站长平台拿验证码」这一步。重新盘点后确认，
本地该做的 5 件事（静态站 / sitemap / llms.txt / IndexNow 双端点推送 / 百度验证标签）全部已跑通并上线。
本次补齐「可补 · 本地直接做」的两项。

线上实测（curl 需 `--noproxy '*'`，否则沙箱代理抖动会造假超时）：
`/` `/scripts/` `/sitemap.xml`(15 条) `/robots.txt` `/llms.txt` `/{key}.txt` 全部 200，content-type 正确。
内容页纯文本量 72,000 字符/页，SEO 底子很扎实。

### 改动

1. **修虚假 lastmod**（`scripts/gen-seo-pages.mjs`）
   - 问题：首页与 `/scripts/` 用 `todayISO()`，每次构建都刷新。Google 会忽略这种「不携带信息量」的
     lastmod，严重时整个字段被降权。内容页用 `script.updatedAt` 是对的，保留。
   - 方案：引入内容指纹 + 持久化缓存。
     - `fingerprint(parts)` = sha256(JSON) 前 16 位
     - `stableLastmod(cache, key, fp)`：指纹未变复用上次日期，变了才推进到今天
     - 首页与剧本库共用 `collectionFp`（剧本 code + updatedAt + hasGuide 序列）
     - 内容页缺 `updatedAt` 时回退到指纹缓存
     - 缓存文件 `.seo-lastmod-cache.json`（已加 gitignore，同 `.indexnow-cache.json`）
   - 顺带把首页/剧本库 `changefreq` 从 daily 改成 weekly（真实更新频率就是周级，解析一部手册才变）。
   - 验证：连跑两次 `gen:seo`，sitemap 的 lastmod 完全一致。

2. **新增 `llms-full.txt`**（513 KB）
   - llms.txt 只有 37 行索引清单，AI 读完知道「该去哪」但还要二次抓取；
     llms-full.txt 把问答对正文（answer 截断 600 字符）+ 故事还原（截断 1500 字符）直接铺开，省掉二次抓取。
   - 数据源是 `details` 里已有的 qa/stories（不额外发请求，不拖慢构建）。注意 `qa` 已被
     `slice(0, MAX_QA_PER_SCRIPT=200)` 截断，文件里如实标注「共 N 条，本文件收录前 M 条」。
   - `llms.txt` 的「内容说明」里加了全文版链接。
   - **故意不把 llms-full.txt 放进 sitemap**：IndexNow 的 URL 来源就是 sitemap 的 `<loc>`，
     推进 Google 索引反可能被判低质量页。它给 AI 抓取器用即可（`/llms.txt` 是约定路径，抓取器会主动找）。

### 环境坑（重要）

- **`APPDATA` 在沙箱 Bash 里是空的** → `npm-conf/lib/defaults.js:31` 在 win32 上走
  `path.resolve(process.env.APPDATA, 'npm-cache')` → `TypeError: paths[0] must be of type string`。
  表现是 `taro build` 报「找不到项目配置文件 config/index」，极误导。
  修法：`APPDATA="C:/Users/Administrator/AppData/Roaming" NODE_OPTIONS= npm run build:h5`。
  用户自己终端里正常，只在沙箱 Bash（尤其后台任务）出现。
- **Edit 工具会「假成功」**：报 success 但没落盘（EBUSY 文件锁的延续）。本次改
  `gen:seo-pages.mjs` 的写文件区时就中招 —— 函数定义了但调用没写进去，跑了构建才发现产物不对。
  **验证不能只信工具返回值，必须 grep / 读文件核对。** 判断依据是产物（dist 里有没有文件），不是日志。

### 待用户完成（需上外网，约 10 分钟）

1. Google Search Console → 添加「网址前缀」`https://www.jbs-ttj.store` → HTML 标签方式 → 把 code 给我
2. Bing Webmaster Tools → **用 Google 账号登录**（微软账号有长期 bug）→ 从 GSC 一键导入
3. 百度站长平台 → 标签早已上线，点「完成验证」即可

拿到 code 后：写进 `src/index.html`（放百度标签旁，SEO 脚本不覆盖）→ 构建 → push → 回 GSC 点验证 →
GSC/BWT 各提交一次 sitemap → BWT 绑定 IndexNow key（否则 Insights 一直显示 get started）。

### 顺带确认

- 工作区 11 个未提交改动 = 微信登录功能（loginWithWechat / 微信错误码映射 / 安全页绑定入口），
  注释与边界处理齐全，可提交。`project.config.json` 的 appid 从 `touristappid` 换成真实
  `wxd733fdcc9cddfac6`，是微信登录必需，正确。
- `npx tsc --noEmit` src/ 0 错；`dist/` 构建前是**小程序产物**，跑 H5 会覆盖，需最后再补一次 weapp 构建。
