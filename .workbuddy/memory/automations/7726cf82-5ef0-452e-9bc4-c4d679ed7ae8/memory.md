# SEO 巡检自动化 — 执行记录

## 2026-09-09 02:00（首次执行）
- HTTPS 探测：key 文件请求 exit 28 超时，HTTP=000；根路径 https=000 / http=200 → **仍未就绪**，与历史结论一致（备案未过）。
- 产物体检：**跳过**。`dist/` 为完全空目录（`ls -A | wc -l` = 0），无 `sitemap.xml`，按护栏规则跳过，未触发 build。
- 结论：https 未就绪 + 体检跳过（dist 空）。

### 经验
- `dist/` 本次是**空目录**而非小程序产物。护栏判据只看 `dist/sitemap.xml` 是否存在，两种情况都落到「跳过」，无需区分。
- 判定 https 时 `-w "%{http_code}"` 得到 000 + exit 28 = 连接超时，比 curl 非零退出码更直观；补一次 http 对照可佐证「只是 https 不通」而非站点整体挂了。
